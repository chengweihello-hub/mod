# opproxy 实现计划

将任意「网页端大模型聊天应用(Webchat)」逆向封装成 OpenAI 兼容标准 API,供外部客户端(Chatbox / Cherry Studio / Cursor / Dify)调用。核心是一套**插件化适配器框架**:新增平台 = 写一个适配器文件,无需改核心代码。

---

## 一、技术选型

| 维度 | 选型 | 理由 |
|---|---|---|
| 运行时 | Node.js 24 | 原生 fetch、内置 WebSocket、性能好,环境已具备 |
| 语言 | TypeScript | 适配器接口契约清晰,是插件化的基础 |
| HTTP 框架 | **Hono** | 轻量、原生 TS、SSE 流式一等公民、中间件简洁 |
| HTTP 客户端 | 原生 fetch + undici | 支持自定义 dispatcher、HTTP 代理、cookie jar |
| 日志 | pino | 轻量结构化 |
| 配置 | JSON 文件 + 环境变量 | 凭证走配置文件,服务参数走环境变量 |
| 测试 | vitest | 验证框架核心与 Mock 链路 |

---

## 二、架构

```
外部客户端 (Chatbox / Cherry Studio / Cursor / Dify)
        │  POST /v1/chat/completions   (OpenAI 格式, 可流式)
        ▼
┌─────────────────────────────────────────┐
│  API Server (Hono)                       │
│  - auth 中间件: 简单 Bearer token         │
│  - /v1/chat/completions (SSE + 非流式)    │
│  - /v1/models                            │
│  - /admin/*  适配器状态 / 凭证校验接口     │
└──────────────────┬──────────────────────┘
                   │ StandardChatRequest (标准化请求)
                   ▼
┌─────────────────────────────────────────┐
│  Adapter Registry (路由层)                │
│  model 名 -> adapter + credential        │
└──────────────────┬──────────────────────┘
                   │
      ┌────────────┼────────────┐
      ▼            ▼            ▼
  ┌──────┐    ┌──────┐    ┌────────┐
  │Mock  │    │Kimi  │    │你新增的│   各 Adapter
  │Adapt │    │Adapt │    │ Adapt  │   实现 WebChatAdapter 接口
  └──┬───┘    └──┬───┘    └───┬────┘
     │           │            │
     ▼           ▼            ▼
  模拟流式   Cookie+内部API  Cookie+内部API
```

**核心数据流(标准化)**:
1. 入站:OpenAI 格式请求 → 剥离无关字段 → `StandardChatRequest`
2. 路由:`req.model` → 找到对应 adapter session
3. 转换:adapter 把 OpenAI messages 翻译成平台内部 payload
4. 调用:adapter 用 cookie/token 请求平台内部 API
5. 出站:adapter 返回 `AsyncIterable<ChatCompletionChunk>` → 服务器统一序列化成 SSE `data: {...}\n\n`
6. **服务器层完全不知道平台差异**,只做 OpenAI 协议的输入输出

---

## 三、核心抽象:适配器接口

分为「适配器(无状态定义)」和「会话(持有凭证)」两层,便于单平台多账号轮询(缓解限流),也为未来多用户扩展留空间。

```typescript
// core/adapter.ts

/** 适配器:无状态,描述能力与凭证 schema */
interface WebChatAdapter {
  readonly name: string;          // 'kimi'
  readonly displayName: string;  // 'Kimi (月之暗面)'
  readonly models: ModelInfo[];   // [{ id:'kimi-k2', context:128000, ... }]
  readonly configSchema: JSONSchema;  // 凭证字段定义 { cookie, token, ... }

  /** 用一份凭证创建会话实例 */
  createSession(config: Record<string, unknown>): AdapterSession;
}

/** 会话:持有凭证,执行实际对话 */
interface AdapterSession {
  /** 校验凭证是否有效 */
  validate(): Promise<boolean>;
  /** 发起对话,返回标准化的流式 chunk */
  chat(req: StandardChatRequest): AsyncIterable<ChatCompletionChunk>;
  /** 可选:凭证刷新(token 续期) */
  refresh?(): Promise<void>;
}
```

适配器只需实现这两个接口,核心代码不依赖任何具体平台。

---

## 四、关键设计

1. **自动扫描装载**:`core/loader.ts` 启动时扫描 `src/adapters/*/index.ts`,自动注册。新增适配器丢进目录即可,无需改注册表。

2. **模型路由**:启动时聚合所有 adapter 的 models,`/v1/models` 返回全集;请求 `model` 字段路由到对应 adapter。支持别名(如 `kimi` == `moonshot-v1`)。

3. **凭证管理**:`config/adapters.json` 存每个适配器的凭证(支持多账号数组)。`configSchema` 声明字段,启动时校验。凭证失效返回明确错误 + 提示。

4. **流式转换工具**:`utils/sse.ts` 提供:
   - `parseSSE(stream)`:把平台各种流式格式(标准 SSE / JSON 流 / 分片)解析成事件
   - `toOpenAIChunk(delta)`:生成标准 OpenAI chunk
   - adapter 只需把平台事件映射成 delta,序列化由服务器统一做

5. **错误处理**:统一错误中间件,把 adapter 抛出的错误转成 OpenAI 风格的 JSON `{error}`,流式中断时发 `data: [DONE]`。

6. **个人自用鉴权**:可选 Bearer token(默认关闭),与客户端配置的 API key 对应。

---

## 五、目录结构

```
opproxy/
├── src/
│   ├── index.ts                  # 入口:加载配置 → 装载适配器 → 启动服务
│   ├── server/
│   │   ├── app.ts                # Hono app 装配
│   │   ├── routes/
│   │   │   ├── chat.ts           # /v1/chat/completions
│   │   │   ├── models.ts         # /v1/models
│   │   │   └── admin.ts          # /admin 适配器状态/凭证校验
│   │   └── middleware/
│   │       ├── auth.ts           # Bearer token 鉴权
│   │       └── error.ts          # 统一错误处理
│   ├── core/
│   │   ├── adapter.ts            # WebChatAdapter / AdapterSession 接口
│   │   ├── registry.ts           # 注册中心 + model→adapter 路由
│   │   ├── types.ts              # StandardChatRequest / ChatChunk / ModelInfo
│   │   ├── credential-manager.ts # 凭证加载 + 多账号轮询
│   │   └── loader.ts             # 自动扫描 src/adapters 装载
│   ├── adapters/
│   │   ├── mock/                 # Mock 适配器(验证整个链路)
│   │   │   └── index.ts
│   │   └── _template/            # 网页端逆向适配器模板(带详细注释)
│   │       └── index.ts
│   └── utils/
│       ├── http.ts               # 带代理/重试/超时的 fetch 封装
│       ├── sse.ts                # SSE 解析 + OpenAI chunk 生成
│       └── logger.ts             # pino
├── config/
│   ├── config.example.json       # 全局配置(端口、鉴权)
│   └── adapters.example.json     # 适配器凭证配置示例
├── docs/
│   ├── architecture.md           # 架构说明
│   ├── adapter-dev-guide.md      # ★ 适配器开发指南(抓包流程 + 开发步骤)
│   └── client-setup.md           # Chatbox/Cherry Studio/Cursor/Dify 接入
├── tests/
│   └── core/registry.test.ts     # vitest 核心测试
├── package.json
├── tsconfig.json
└── README.md
```

---

## 六、实现步骤(分 4 阶段)

### 阶段 1:项目骨架 + 核心抽象
1. 初始化:`package.json`、`tsconfig.json`、装依赖(hono、undici、pino、zod、vitest)
2. `core/types.ts`:`StandardChatRequest`、`ChatCompletionChunk`、`ModelInfo`
3. `core/adapter.ts`:`WebChatAdapter`、`AdapterSession` 接口定义
4. `core/registry.ts`:注册中心 + model→adapter 路由
5. `utils/sse.ts`:SSE 解析与 chunk 生成工具

### 阶段 2:API 服务器
6. `server/app.ts`:Hono 装配
7. `middleware/auth.ts` + `middleware/error.ts`
8. `routes/models.ts`:`/v1/models` 聚合
9. `routes/chat.ts`:`/v1/chat/completions`(流式 SSE + 非流式 JSON)
10. `core/credential-manager.ts`:配置加载、多账号轮询

### 阶段 3:Mock 适配器 + 模板 + 自动装载
11. `adapters/mock/index.ts`:模拟流式输出,验证整条链路
12. `adapters/_template/index.ts`:完整逆向适配器骨架(见下节)
13. `core/loader.ts`:自动扫描装载
14. `routes/admin.ts`:适配器状态、凭证校验接口

### 阶段 4:文档 + 收尾
15. `docs/adapter-dev-guide.md`:抓包流程 + 适配器开发步骤
16. `docs/client-setup.md`:各客户端接入示例
17. `README.md`:快速开始
18. 跑通 Mock 适配器的端到端测试

---

## 七、适配器模板要点(`_template/index.ts`)

模板是本项目的核心交付物,包含一个网页端逆向适配器的完整骨架,带详细注释与 TODO 标注:

- **configSchema**:声明凭证字段(如 `cookie`、`userToken`)及说明
- **models**:声明该平台支持的模型 id
- **createSession**:
  - `validate()`:请求平台某个轻量接口校验凭证
  - `chat(req)`:核心流程
    1. OpenAI `messages` → 平台内部 payload(含 conversation 管理)
    2. 用 `utils/http` 带 cookie 请求平台内部 API
    3. 用 `utils/sse.parseSSE` 解析平台流式响应
    4. 映射成标准 `ChatCompletionChunk` yield
  - `refresh()`:token 续期逻辑(若有)

模板内嵌抓包指引注释:用浏览器 DevTools Network 面板抓 `/completion` 类请求,复制 Cookie、端点、payload 结构到对应位置。

---

## 八、Mock 适配器的作用

- **可运行验证**:不依赖任何真实平台,启动即用,验证从 `/v1/chat/completions` 到 SSE 序列化、错误处理、模型路由的**整条链路**确实工作
- **开发参考**:展示一个最小但完整的 `WebChatAdapter` 实现
- 用于 `tests/` 中的端到端测试

---

## 九、本次不实现的内容(明确边界)

- **不直接实现具体真实平台的逆向适配器**(网页端内部 API 会随平台更新变化,避免提供可能过时/不准确的协议细节)
- 改为提供**结构完整、注释详尽的模板** + **抓包开发文档**,你按指引抓包后填入模板即可得到可用适配器
- 如后续你已抓到某平台协议,我可据此帮你实现具体适配器

---

## 十、预期可交付

- 一个 `npm run dev` 即可启动的 OpenAI 兼容 API 服务
- Mock 适配器跑通完整链路(可直接用 Chatbox 连 `http://localhost:3000/v1` 测试)
- 适配器模板 + 开发文档,支撑你逆向任意平台
- 各客户端接入说明文档
<img width="2505" height="989" alt="image" src="https://github.com/user-attachments/assets/380e57ff-6ea8-477a-9ecf-088c86195844" />
